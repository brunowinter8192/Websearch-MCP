# CDP Headed Probe (Milestone 1b) — 20260822_182451

Dev-only probe (macOS). `crawl4ai==0.9.2`, `patchright==1.61.2`. Bundle: `/Users/brunowinter2000/Library/Caches/ms-playwright/chromium-1228/chrome-mac-arm64/Google Chrome for Testing.app`.

## 1. Self-launch + CDP endpoint

- `open -g -n -a` launch issued: True
- Error (if any): none
- CDP HTTP `/json/version` reachable: True
- Detail: Chrome/149.0.7827.55

## 2. crawl4ai connect + scrape over cdp_url

Config shape used: `BrowserConfig(cdp_url=f"http://127.0.0.1:{port}", browser_mode="custom", enable_stealth=True, cdp_cleanup_on_close=True)` + `UndetectedAdapter()` + `AsyncPlaywrightCrawlerStrategy`. `headless` field is DEAD on this path (never read inside `browser_manager.py`'s `cdp_url` branch — confirmed by reading the source, not just inferred) — headed-ness comes entirely from how we spawned the process.

- Scrape success: True
- Content length: 335
- Error (if any): Blocked by anti-bot protection: Structural: minimal_text on small page (335 bytes, 16 chars visible)

## 3. Focus poll (whole sequence: self-launch -> connect -> page creation -> navigation -> teardown)

**Headline figure is the ROUTE UNDER TEST ONLY** (`self_launch` + `cdp_port_wait` + `cdp_connect_page_navigate` + `teardown`) — EXCLUDES `reference_launch`, which is this script's own internal tooling step (a direct, un-backgrounded patchright launch captured only for the cmdline diff in section 4, not part of the cdp_url route and not expected to avoid focus steal — folding it into the headline would misrepresent the route being tested):

- Route samples: 8
- Route Chrome-frontmost count: 0 (0%)
- Distinct apps seen (whole run, all stages): ['Google Chrome for Testing', 'ghostty']

**Per-stage breakdown** (attributes any Chrome-frontmost hit to WHICH step caused it — `cdp_connect_page_navigate` bundles connect/`get_page()`/goto, the page-creation-over-CDP moment playwright#42343 flags; finer sub-staging inside it would require hooking crawl4ai internals, out of scope here). A stage showing 0 samples means it completed faster than the 0.25s poll interval — NOT that it was confirmed focus-clean, just unsampled. Table order is thematic (route stages grouped first), NOT chronological — `reference_launch` actually runs FIRST in real execution, before any route stage:

| Stage | in route? | samples | chrome frontmost | % |
|-------|-----------|---------|-------------------|---|
| self_launch | yes | 0 | 0 | n/a |
| cdp_port_wait | yes | 1 | 0 | 0% |
| cdp_connect_page_navigate | yes | 6 | 0 | 0% |
| teardown | yes | 1 | 0 | 0% |
| reference_launch | NO (reference/tooling) | 10 | 8 | 80% |

## 4. cmdline delta vs. patchright reference (headed, probe 04 Run B shape)

- Reference launch error: none
- Reference PID/exe: 39426 / `/Users/brunowinter2000/Library/Caches/ms-playwright/chromium-1228/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing`
- Self-launch cmdline captured: True
- Flags common to both: 4
- **Only in reference (patchright-owned surface we would NOT get on this route):** ['--disable-background-networking', '--disable-background-timer-throttling', '--disable-backgrounding-occluded-windows', '--disable-blink-features', '--disable-breakpad', '--disable-component-update', '--disable-dev-shm-usage', '--disable-domain-reliability', '--disable-edgeupdater', '--disable-features', '--disable-field-trial-config', '--disable-gpu', '--disable-gpu-compositing', '--disable-hang-monitor', '--disable-infobars', '--disable-ipc-flooding-protection', '--disable-prompt-on-repost', '--disable-renderer-backgrounding', '--disable-search-engine-choice-screen', '--disable-software-rasterizer', '--disable-sync', '--edge-skip-compat-layer-relaunch', '--enable-features', '--export-tagged-pdf', '--force-color-profile', '--ignore-certificate-errors', '--ignore-certificate-errors-spki-list', '--mute-audio', '--no-sandbox', '--no-service-autorun', '--password-store', '--remote-debugging-pipe', '--use-mock-keychain', '--window-position', '--window-size']
- Only in self-launch (ours, not patchright's): ['--remote-debugging-port']

## Teardown

Orphan processes/launchd jobs after the full probe: 0